"use strict"

// fakes from /dbmongo/js/test/public/lib_public.js

function ISODate(date) {
  return new Date(date)
}

const f = this /* = {
  altaresToHuman,
  apconso,
  apdemande,
  bdf,
  compareDebit,
  cotisations,
  dateAddDay,
  dateAddMonth,
  dealWithProcols,
  debits,
  delai,
  diane,
  effectifs,
  finalize,
  flatten,
  generatePeriodSerie,
  idEntreprise,
  iterable,
  map,
  procolToHuman,
  reduce,
  sirene,
  ...
}*/

const exports = { f }
